using UnityEngine;
using System.Collections;

public class Controller : MonoBehaviour {

	public bool exercise_one = false;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {

		/*if (Fighter.S.transform.position.x >= 1) {
			Timer.S.FreezeFighter();
			Timer.S.time = 10;
			ExerciseText.S.goText.text = "Do 50 Jumping Jacks";
		}*/
	
	}
}
